PlaneSpotter shows aircrafts for your locations.

The data is provided by ADS-B One. Map data is provided by OpenStreetMap under the Open Database License (ODbL).

Minimum permissions:
PlaneSpotter only requires a minimum of permissions, namely only the INTERNET permission.
This is necessary to make HTTP requests to remote servers to receive data.
Optional: Authorization for GPS.
